<!DOCTYPE html>
<html lang="en">
    <%- include("../partials/head.ejs") %> 

<body>
    <%- include("../partials/header.ejs") %> 
    <main>
        <h1> Edit Animal</h1>
        <div>
            <form action="/animals/<%= animal._id %>?_method=PUT" method="POST" >
            
                <fieldset>
                    <legend>Edit a <%= animal.species %></legend>
                    <label>
                        SPECIES: <input 
                            type="text"
                            name="species"
                            value="<%= animal.species %>"
                            placeholder="enter a animals species"
                        />
                    </label>
                    <label>
                        EXTINCT: <input type="checkbox" name="extinct" <%= animal.extinct ? "checked" : "" %> />
                    </label>
                    <label>
                        LOCATION: <input 
                            type="text"
                            name="location"
                            value="<%= animal.loctaion %>"
                            placeholder="enter the location"
                        />
                    </label>
                    <label>
                        LIFE EXPECTANCY: <input 
                            type="text"
                            name="lifeExpectancy"
                            value="<%= animal.lifeExpectancy %>"
                            placeholder="enter the life expectancy"
                        />
                    </label>
                    
                </fieldset>
                <input type="submit" value="Update <%= animal.species%>" />
            </form> 

        </div>

    </main>

</body>

</html>

